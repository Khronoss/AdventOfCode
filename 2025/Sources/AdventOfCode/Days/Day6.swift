import Parsing
import Foundation

struct Day6 {
    let identifier = "Day6"

    func run(from filePath: URL) throws -> Int {
        let inputText = try String(contentsOf: filePath, encoding: .utf8)

        return 0
    }
}
