import Parsing
import Foundation

struct Day5 {
    let identifier = "Day5"

    func run(from filePath: URL) throws -> Int {
        let content = try String(contentsOf: filePath, encoding: .utf8)

        return 0
    }
}
